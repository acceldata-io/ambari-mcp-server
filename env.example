# Ambari Server Configuration
AMBARI_BASE_URL=https://10.100.11.39:8446/api/v1
AMBARI_USERNAME=admin
AMBARI_PASSWORD=admin
AMBARI_CLUSTER_NAME=rl8_odp3003

# Optional: Request timeout in milliseconds (default: 30000)
TIMEOUT_MS=30000

# Optional: Skip SSL certificate verification (for self-signed certs)
# Set to 1 or true to skip SSL verification
INSECURE_SSL=1

# Optional: Debug mode (set to 1 to enable verbose logging)
DEBUG=0
ENV_DEBUG=0

# ============================================================================
# SSH Configuration (for remote command execution on cluster nodes)
# ============================================================================

# Path to SSH private key file (.pem) for connecting to cluster nodes
# Can be absolute path or relative to the project root
SSH_PRIVATE_KEY_PATH=/Users/basapuramkumar/Downloads/dc_odp_team

# SSH username (default: root)
SSH_USERNAME=acceldata

# SSH port (default: 22)
SSH_PORT=22

# SSH connection timeout in milliseconds (default: 10000)
SSH_TIMEOUT=10000

# ============================================================================
# Kubernetes Configuration (for Ambari clusters running on K8s)
# ============================================================================
# Use these settings when your Ambari agents run in Kubernetes pods
# instead of traditional VMs/hosts.

# Path to kubeconfig file (optional, uses default ~/.kube/config if not set)
# K8S_KUBECONFIG_PATH=/path/to/kubeconfig

# Kubernetes namespace where Ambari pods are running (default: default)
K8S_NAMESPACE=default

# Label selector to identify Ambari agent pods
# This is REQUIRED to enable K8s mode (e.g., "app=ambari-agent" or "component=ambari-agent")
# K8S_POD_LABEL_SELECTOR=app=ambari-agent

# Container name in the pod (optional, uses first container if not set)
# K8S_CONTAINER_NAME=ambari-agent

# Command execution timeout in milliseconds (default: 30000)
K8S_TIMEOUT=30000
